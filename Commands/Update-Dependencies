#! /bin/bash

set -x
set -euo pipefail

for projectVariant in Default AppStore DeveloperID; do
    xcodegen -s XcodeGen/Variant/"$projectVariant".yml -r .
done

for i in TMBuddy-*.xcodeproj; do
    xcodebuild -resolvePackageDependencies -project "$i"
done
