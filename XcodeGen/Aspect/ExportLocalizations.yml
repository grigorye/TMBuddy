targets:
  TMBuddy:
    settings:
      SWIFT_EMIT_LOC_STRINGS: YES
      MACOSX_DEPLOYMENT_TARGET: 12.0

  TMBuddyFinder:
    settings:
      SWIFT_EMIT_LOC_STRINGS: YES
      MACOSX_DEPLOYMENT_TARGET: 12.0

  TMBuddyTMUtilHelper:
    settings:
      CREATE_INFOPLIST_SECTION_IN_BINARY: NO # Patch it to make xcodebuild not fail due to missing Processed-Info.plist
      OTHER_LDFLAGS:
